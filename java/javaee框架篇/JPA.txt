dao层接口
public interface UserTxnRepo extends JpaRepository<UserTxn, String>{}	--第二个参数为主键数据类型

1.自动类型
public List<UserTxn> findByUserIdAndTxnType(String user_id, String txn_type);
实体类属性命名规则：驼峰规则
userId
txnType


2.自定义类型
注意：只能select * ，不能select 字段，否则跟实体类对应不上
nativeQuery=true表示使用标准sql语句
@Query(value="SELECT max(create_dt) FROM user_txn where user_id = ?1 and txn_type= ?2",nativeQuery=true)
public Date findLastTxnDate(String user_id, String type);