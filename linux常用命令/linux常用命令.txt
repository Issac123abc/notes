ctrl+c	--退出错误指令

查看文件或文件夹
1.ls	ls -a	ll

查看目录路径
pwd

查看磁盘使用情况
df -h

创建、删除空文件夹
mkdir 名
rmdir 名	--只能删除空文件夹

创建空文件
touch hello.txt hello1.txt	--可以创建多个文件，空格隔开

删除
rm -rf 文件名或者目录	-r代表递归	-f表示强制，不提示

拷贝
cp 文件名 目录
cp -r 文件夹 目录
\cp	强制覆盖

移动（剪切）
mv a.txt b.txt		--表示文件或文件夹重命名，
mv a.txt test/		--表示移动文件到指定目录

浏览文件
cat -n 文件名 | more	--n表示行号，more表示分页，空格翻页
more	vi全屏显示
less	动态加载内容，效率高
head 文件名		显示前10行
head -n 5 文件名	显示前5行
tail 文件名		显示最后10行
tail -n 文件名		显示最后5行
tail -f 文件名		实时监控文件变化

覆盖写入与追加（>、>>）
ll > a.txt			ll >> a.txt
cat a.txt > b.txt		cat a.txt >> b.txt
echo "hello echo" > c.txt	echo "hello echo" >> c.txt
cal > d.txt			cal >> d.txt

添加软链接、历史指令
ln -s 目录或文件 软链接名称
rm -rf 软链接名称
history		--本用户所有历史指令
history 10	--最近10指令
!178		--执行第178条指令

时间与日历
date
date -s 日期时间
cal
cal 2019

查找
find 目录 -name 文件名		--按文件名查找
find 目录 -user 用户名		--按用户名查找
find 目录 -size 20m		--按文件大小查找（+大于，-小于，没符号=）
cat hello.java | grep -ni hello	--n表示显示行号，i表示忽略大小写

压缩与解压
zip -r 压缩文件名 目录		--将目录下文件压缩成zip文件
zip 压缩文件名 a.txt b.txt	--将多个文件添加到压缩文件
unzip 压缩文件名		--解压到当前目录
unzip -d 目录 压缩文件名	--解压到指定目录

tar -zcvf 压缩文件名 目录或多个文件	--压缩
tar -zxvf 压缩文件名			--解压到当前目录
tar -zxvf 压缩文件名 -C 目录		--解压到指定目录

vim
1.正常模式
yy 5yy	复制
p	粘贴
dd 5dd	删除
G	末行
gg	首行

2.插入模式

3.命令模式
:wq q q!
/key	查找	n下一个
:set nu/nonu	显示行号


关机、重启、断开连接
halt	--立即关机
reboot	--重启
sync	--同步内存数据到磁盘
logout	--断开连接

用户管理
1.useradd -g 组名 用户名		--创建用户
2.passwd test				--设置密码
3.usermod -g 组名 用户名		--修改用户组
4.userdel test				--删除用户但保留家目录
5.userdel -r test			--删除用户并删除家目录
6.id 用户名				--查看用户信息
7.su - test				--切换用户，高权限向低权限切换不需要密码，反之则需要
8.exit					--退出当前用户

组管理
1.groupadd 组名
2.groupdel 组名

cat /etc/group 		--查看用户组
cat /etc/passwd 	--查看用户信息

权限
修改文件用户组
1.chown 用户名 文件或目录
2.chgrp 组名 文件或目录

文件类型
-普通文件
-d目录
-b硬盘
-c字符文件（鼠标，键盘）
-l 软链接





